@model List<Application.ViewModels.GenreViewModel>
@{
    ViewData["Title"] = "Genre";
}

<div class="container">
    <div class="row mb-3">
        <div class="col-3 offset-9">
            <a asp-controller="Genre" asp-action="Create" class="btn btnNuevo">Crear Nuevo Genero</a>
        </div>
    </div>
    <div class="row">
        @if (Model == null || Model.Count == 0)
        {
            <h2>No hay Genre !</h2>
        }
        @foreach (Application.ViewModels.GenreViewModel item in Model)
        {
            <!-- Repite este bloque para cada serie -->
            <div class="col-md-3">
                <div class="card mb-4 shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">@item.Name</h5>
                        <div class="d-flex justify-content-between align-items-center">
                            <div class="btn-group">
                                <a asp-controller="Genre" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">Editar</a>
                                <a asp-controller="Genre" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">Borrar</a>
                            </div>
                        </div>
                    </div>
                </div>

            </div>

            <!-- Fin del bloque -->
        }
    </div>
</div>
